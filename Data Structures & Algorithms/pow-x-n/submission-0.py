class Solution:
    def myPow(self, x: float, n: int) -> float:
        total = 0
        total = x ** n
        return total
        # for _ in range(n):
            
        #     if n < 0 :
        #         total = total ** 1/x
        #     total = total ** x
